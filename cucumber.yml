default: --publish-quiet --format pretty --strict --tags "not @requires_bearer_token"
api: --publish-quiet --format pretty --strict --tags "@api and not @requires_bearer_token"
bearer: --publish-quiet --format pretty --strict --tags "@bearer_token"
ui: --publish-quiet --format pretty --strict --tags "@ui"
